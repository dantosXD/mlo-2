// Integration with external income verification API
async function fetchIncomeHistory(ssn) {
  // Placeholder for external API call
  return { ssn, annualIncome: 90000 };
}

module.exports = { fetchIncomeHistory };
